<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/customer/css/register.css" />
</head>

<body>
    <!-- Nhúng file JavaScript -->
   
    
    <form action="/login/register" method="POST" onsubmit="return validate()">
        <div class="register">
            <h1>Đăng ký</h1>
            <p>Vui lòng điền thông tin để đăng ký</p>
            <hr>
            <label for="username"><b>Tên đăng nhập</b></label>
            <input type="text" placeholder="Họ và tên" name="user" id="username">
            <label for="password"><b>Mật khẩu</b></label>
            <input type="password" placeholder="******" name="password" id="password">
            <label for="password-repeat"><b>Nhập lại mật khẩu</b></label>
            <input type="password" placeholder="******" name="confirmPassword" id="password-repeat">
            <label for="password-repeat"><b>Email</b></label>
            <input type="email" placeholder="Địa chỉ email" name="email" id="password-repeat">
            <hr>
            <p>Để tạo tài khoản vui lòng đồng ý với điều khoản của chúng tôi <a href="#">Terms & Privacy</a>.</p>
            <button type="submit" class="submit">Đăng ký</button>
        </div>
        <div class="register login">
            <p>Bạn đã có tài khoản rồi? <a href="/login">Đăng nhập</a>.</p>
        </div>
    </form>
    <script language="javascript" src="/customer/script/kiemtra_register.js">
        
    </script>
    <% if(message == "Đăng ký thành công" ){ %>
        <script>
            if(confirm("Đăng ký thành công.OK để chuyển sang đăng nhập") == true) {
                window.location.href = "/login";
            }
        </script>
    
    <% } else if(message) { %>
        <script>
            alert("<%= message %>")
        </script>
    <% } %>
</body>

</html>