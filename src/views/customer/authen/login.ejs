<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/customer/img/logo.png" type="image/x-icon">
    <title>Furni</title>
    <link rel="stylesheet" href="/customer/css/login.css">
    <style>
        #toast {
            position: fixed;
            top: 0;
            right: 0;
            /* background-color: antiquewhite; */
            width: 400px;
            z-index: 9999;
        }
        
        #toast .notification {
            background-color:white ;
            height: 90px;
            margin: 20px 10px;
            border-radius: 5px;
            display: flex;
            box-shadow: 0 0 5px rgb(143, 142, 142);
            position: relative;
            animation: toast-in ease .3s,toast-out ease .5s 3s forwards;
            transition:all 0.5s ease;
        }
        
        @keyframes toast-in {
            from {
                transform: translateX(calc(100% + 10px))
            }
            to {
                transform: translateX(0)
            }
        }
        
        @keyframes toast-out {
            from {
                opacity: 1
            }
            to {
                opacity: 0
            }
        }
        
        #toast .notification.success {
            border-left: 4px solid greenyellow;
        }
        
        #toast .notification.error {
            border-left: 4px solid rgb(255, 89, 47);
        }
        
        #toast .notification.info {
            border-left: 4px solid rgb(91, 91, 184);
        }
        
        #toast .notification .icon {
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            width: 40px;
        }
        
        #toast .notification.success .icon {
            color: greenyellow;
        }
        
        #toast .notification.error .icon {
            color: rgb(255, 89, 47);
        }
        
        #toast .notification.info .icon {
            color: rgb(91, 91, 184);
        }
        
        #toast .notification .noti {
            margin: auto 0;
            width: calc(100% - 40px - 40px);
        }
        
        #toast .notification .noti .title {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 3px;
        }
        
        #toast .notification .noti .message {
            color: rgb(167, 169, 170);
            font-size: 13px;
            width: calc(100%);
            max-height: 40px;
            overflow-y: hidden;
            
        }
        
        #toast .notification .close {
            position: absolute;
            right: 0px;
            width: 40px;
            text-align: center;
            line-height: 90px;
            color: rgb(167, 169, 170);
            font-size: 18px;
        }
        
    </style>
</head>

<body>
    <div id="toast"></div>
    <div class="sign-up">
        <div class="circle circle--red"></div>
        <div class="circle circle--yellow"></div>
        <div class="circle circle--green"></div>
        <div class="circle circle--purple"></div>
        <form class="sign-up__form" id="form-login" action="#">
            <div class="sign-up__content">
                <h2 class="sign-up__title">Đăng nhập</h2>
                <input class="sign-up__inp" type="text" placeholder="Tên đăng nhập" required="required" name="username" id="username"/>
                <input class="sign-up__inp" type="password" placeholder="Mật khẩu" required="required"
                    name="password" id="password" />
                <a class="forgot__password">Quên mật khẩu</a>
            </div>
            <div class="sign-up__buttons">
                <a class="btn btn--register" href="/login/register">Tạo tài khoản</a>
                <button class="btn btn--signin" id="submit">Đăng nhập</button>
            </div>
        </form>
    </div>
</body>
<script src="/customer/scripts/login.js"></script>
<script src="/customer/scripts/custom-dev.js" ></script>
</html>